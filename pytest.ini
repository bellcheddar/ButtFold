[pytest]
testpaths = tests
markers =
    slow: runs the real fold binary and the real baker; needs numpy and takes seconds
# The web layer's tests must run without numpy, because the droplet's web layer has no
# numpy: that is the whole reason the queue worker is a separate process. `-m "not slow"`
# is the fast suite; tools/check_all.sh runs everything.
